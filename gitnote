
git remote add origin https://github.com/vta-itsec/Agentic-RAG-chat.git
git branch -M main
git push -u origin main

# 1. Trước khi sửa: pull code mới nhất
git pull origin main
# 2. Sửa file
# 3. Commit
git add .
git commit -m "Commit"
# 4. Pull lần nữa để tránh xung đột
git pull origin main
git push -u origin feature/saas-refactor
# 5. Push lên remote
git push origin main

git pull origin main --allow-unrelated-histories # Chạy lệnh Pull với cờ cho phép hợp nhất lịch sử

# ============================================
# SaaS Refactor Progress (Dec 2025)
# ============================================

✅ Completed:
- Docker Compose mới với microservices architecture
- LibreChat config (Enterprise RAG endpoint)
- API service chạy thành công
- Fix CORS, pytest, MongoDB compatibility issues
- Comprehensive .gitignore

⏳ In Progress:
- Resolve merge conflicts
- Test LibreChat UI
- RAG service implementation
